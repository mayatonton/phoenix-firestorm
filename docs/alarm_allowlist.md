# alarm allow-list(fail-closed の唯一の逃がし穴・AYA 権限専用)

CLAUDE.md 冒頭「🔒 憲法」の default-deny gate が参照する唯一の allow-list。
**ここに明示されたシグナルだけが PASS を妨げない。それ以外の全 log alarm は自動でブロック(fail-closed)。**

## 運用ルール(不変)
- **追記できるのは AYA(人間)のみ。** agent はこのファイルに追記・編集してはならない(憲法 §4 = 凍結対象)。
- 1 エントリに **理由**必須。理由なき登録は無効。
- 登録は「バグを消した」ではなく「このシグナルは当面 PASS を妨げないと AYA が判断した」の記録。可能なら TICKET/期限を併記。
- alarm が根治したら該当行を削除(allow-list は最小を保つ)。

## 書式
```
| signal (grep パターン/一意キー) | 分類: ACCEPTED / KNOWN-BUG(#ref) | 理由 | 登録者 | 日付 |
```

## 登録済み

> r43 upstream 統合(feature/ayastorm-r43-upstream-sync)の validation 起動(`AYASTORM_VK_VALIDATION=1`)で観測された pre-existing alarm を AYA 裁定で登録(2026-07-24)。いずれも「r43 merge が触れていない VK-infra/skinning/RT/network 経路」で、merge 導入の新規 alarm ではないことを確認済(UBOReg/BindReg violation=0・merge-touched code の error/warning=0)。

| signal | 分類 | 理由 | 登録者 | 日付 |
|---|---|---|---|---|
| `abort: fail harder status Http_403`(lltexturefetch.cpp:1824 doWork) | ACCEPTED | CDN が per-asset に返す 403 Forbidden。request 経路は stock upstream(:1414-1427・改修なし)=非自 bug。走行実測 = 412 中 52 asset(12.6%)のみ 403・残は正常ロード=全 fetch 一律でない=server 認可判断。retry は有界(ASSET_RETRY_LIMIT=8 + 指数 backoff 4→512s → setIsMissingAsset で停止・llviewertexture.cpp:2092-2121 / llassetretry.h:8)=暴走なし。403 以外の status を持つ fail harder は本 pattern に match せず未承認 alarm として残る(=毎回 403 確認) | AYA | 2026-08-06 |
| `CURL GET FAILED, status: Http_403`(lltexturefetch.cpp:2237 onCompleted) | ACCEPTED | 上記 fail harder と同一 failure の別 WARNING(1 失敗=2 行)。同一 CDN per-asset 403。非 403 status は本 pattern に match せず alarm 残置(map tile は :2235 で本行 skip) | AYA | 2026-08-07 |
| `http fail acc: 403=`(lltexturefetch.cpp:2275 onCompleted) | ACCEPTED | 403 失敗の 10 秒毎有界集計サマリ(enumerator・大量失敗の可視化装置=残す)。per-event の 403 判別は :1824/:2237 が担うため本サマリ allow は非 403 を隠さない(サマリは 404=/5xx=/other= を別欄でそのまま出す) | AYA | 2026-08-07 |
| `#VKContract#` の `cause{...fb_view_diffuse` / `fb_view_aux` / `fb_heap_default...}` | ACCEPTED | RT texture-sample の benign note(失敗 verdict でない・既 disposition) | AYA | 2026-07-24 |
| `#GLTF# ... Error getting material asset data: Asset request: failed (-1)`(llgltfmateriallist.cpp) | ACCEPTED | material asset の fetch miss(network/content・code 起因でない) | AYA | 2026-07-24 |
| `LLSDXMLParser::Impl::parse: XML_STATUS_ERROR parsing:Not [Ff]ound`(llsdserialize_xml.cpp) | ACCEPTED | asset 応答 "Not Found" を LLSD parse した miss(network・自 xml 起因でない) | AYA | 2026-07-24 |
| `#MESHSKININFO# non-finite inverse_bind_matrix at decode`(llmodel.cpp fromLLSD) | ACCEPTED | 壊れた rigged mesh の content 問題(asset 内に NaN/Inf の inverse_bind_matrix)。code は decode 時に identity へ sanitize(cap20 warn・1回/mesh)・自 bug でない。根治 = commit `309655402c8`(旧「#VKNaN# non-finite skin palette」per-frame log は消滅=根治済で本表から削除)。診断で Class 2 joint torn-read は誤診と判明(worldNaN=0/ibmNaN=1) | AYA | 2026-07-24 |
| `UNASSIGNED-Threading-MultipleThreads-Read` / `-Write`(VK_OBJECT_TYPE_FENCE の vkGetFenceStatus/vkResetFences) | ACCEPTED | 我々の VK fence 同期設計(main+worker が fence status を並行 poll)を validation layer が指摘。設計上の並行 access で joint/merge とは無関係 | AYA | 2026-07-24 |
| `UNASSIGNED-CoreValidation-DrawState-InvalidImageLayout` | ACCEPTED | VK infra の RT/texture image layout 遷移(validation-layer のみ・視覚正常)・merge 未 touch | AYA | 2026-07-24 |
| `VUID-vkAcquireNextImageKHR-surface-07783` | ACCEPTED | swapchain acquire の forward-progress 警告(present-mode/FRAMES_IN_FLIGHT 由来)・merge 未 touch | AYA | 2026-07-24 |
| `VUID-vkCmdDrawIndexedIndirect-renderpass` | ACCEPTED | shutdown/teardown 時の draw(saveSnapshot→pool closing 近傍)・clean 終了経路・merge 未 touch。※final snapshot render は commit `73a10e82e04` で validation 時 skip 化 | AYA | 2026-07-24 |
| `VUID-vkDestroyDevice-device-05137` | ACCEPTED | device 破棄時の benign owner leak(memory 台帳既載・force-release 禁止で恒久受容)。clean shutdown 到達で表面化(task#13 根治後) | AYA | 2026-07-24 |
| `VUID-VkImageMemoryBarrier-oldLayout-01212` / `VUID-vkCmdCopyImageToBuffer-srcImage-00186` | ACCEPTED | avatar bake placeholder readback の既知対(CLAUDE.md 残 alarm register A =「塞ぐな」裁定済)。bake readback 実行時のみ出現(run 状況依存・2026-07-25 gate run で file 未登録と判明し正式登録) | AYA | 2026-07-25 |
| `#VKContract#` の `fbslot{...Deferred Skinned Shadow Shader...:no_view}` | ACCEPTED | shadow 深度パスで rigged shadow shader が宣言する未使用 global sampler(b24-37=probe/env 系)に fallback view を当てた benign note。深度シェーダは当該 sampler を読まない=視覚無害(gate 視覚OK 2026-07-30)。worker 撤去(shadow mt path 直列化)で rigged shadow が main の通常解決を通り per-draw 顕在化しただけ(旧 mt は worker seed が解決を bypass)。旧ログ `AYAstorm.old`(変更前)に skinned shadow no_view 不在=新規顕在化を確認。※`Material Shader 0` / `Deferred SpotLight Shader` の no_view は pre-existing(旧ログに既存)=本エントリ対象外・別途裁定 | AYA | 2026-07-30 |
| `global position is still unknown`(lllandmarklist.cpp:241 onRegionHandle) | ACCEPTED | landmark が指す region を grid が解決不能(RegionHandle=0 = region 消滅/offline)な時に per-landmark で出る benign note。全枝トレース: 全解決は mRegions(server reply=processRegionIDAndHandle が insert 後 callback 発火)経由で WARN 到達時 entry は必ず存在 → 失敗因は `from_region_handle` の g_x>0 && g_y>0 判定(llregionhandle.h:117-118 / lllandmark.cpp:72)= handle==0 のみ(mLocalRegion 経路は setRegionHandle 呼出元ゼロで死枝)。∴ live region は有効 handle で必ず解決=誤発火不能=保存すべき defect 検出器なし。viewer は正しく要求し grid が「region 無い」と回答しただけ=非自 bug。基準走行 127 件=全 distinct dead landmark を起動時 SLURL 一括キャッシュ(llfavoritesbar getSLURL:1948)した 1 秒 burst。upstream 由来(commit ad2bee19e0ea・Ansariel 2024-05-22)・VK 非関係 | AYA | 2026-08-07 |
| `Volume group has remaining refs`(llvolumemgr.cpp:274/280/285 LLVolumeLODGroup::cleanupRefs)+ `Remaining references in the volume manager`(llappviewer.cpp:2498) | ACCEPTED | 終了時のみの benign accounting WARN(実メモリ leak でない)。全枝トレース: cleanup()/cleanupRefs() の呼出元は ~LLVolumeMgr と shutdown(llappviewer.cpp:2496 + cleanupVolumeManager :2500/:2510)のみ = TP/region-change 経路ゼロ。順序倒錯 = volume_manager->cleanup()(:2496)が drawable teardown(gPipeline.cleanup=shutdownGL :2685)より先に走るため、drawable が握る生存 object が volume ref を保持したまま検出される。ref は有効(生存 object が正当保持)で、その object destruct 時に ~LLPrimitive→unrefVolume で正常解放される(runtime/TP では逐次解放=蓄積なし・件数がシーン依存で session 長非依存=蓄積 leak でない署名)。直接 refVolume caller(spatialpartition/viewerwindow 物理・meshrepo)は全て局所 unref で不均衡なし。cleanupRefs が残 ref を force-clear(:275-282)して継続 = crash なし。exit-fix `_exit(0)` で静的破棄 bypass 済。upstream 由来(killAllObjects assert が LLVOAvatarSelf never-dies を明記)・VK 非関係。修正(cleanup を :2692 後へ移す順序変更)は upstream teardown 乖離ゆえ D3=allowlist 採択(実害ゼロ・AYA 裁定 2026-08-07) | AYA | 2026-08-07 |
| `HTTP request .* failed after .* retries.  Reason:  (Service Unavailable \(Http_503\)|Not Modified \(Http_304\)|Timeout was reached \(Easy_28\)|Couldn't resolve host name \(Easy_6\))`(_httppolicy.cpp:404 stageAfterCompletion) | ACCEPTED | 外部 server/network 由来の HTTP 失敗(全て bounded retry・上限 `HTTP_RETRY_COUNT_DEFAULT=5`・_httppolicy.cpp:393/_httpinternal.h:130)。503=aditi 素材 CDN の一時的 Service Unavailable(E-08 403 と同 class・server-side)/ Easy_28=sim cap POST timeout / Easy_6=DNS 解決失敗 / 304=FSData 起動時更新確認の条件付き GET に対する「cache 最新」正常応答で viewer は正しく処理(fsdata.cpp:284「no newer version available」)・低層が failure 誤分類しているだけ(HttpStatus operator bool が 2xx のみ success・httpcommon.h:352)。request URL 正当(?material_id / cap / fsdata xml)= 不正 request でない。**E-04 教訓に従い汎用 logger 丸ごとでなく特定 status に限定** = 未知の http 失敗は不 match で alarm 残置。isRetryable=499-599(httpcommon.cpp:254) | AYA | 2026-08-07 |
| `Possible failure \[(Http_503|Easy_28|Http_304|Easy_6)\] cannot (GET\|POST)`(llcorehttputil.cpp:276 onCompleted) | ACCEPTED | 上記 _httppolicy:404 と同一失敗の coroutine 層報告(E-08 fail-harder 2 層同型)。canceled request(HE_HANDLE_NOT_FOUND)は :264 で除外済。特定 status に限定 = 未知 status は不 match で残置 | AYA | 2026-08-07 |
| `Asset .* request aborted for .*`(llassetstorage.cpp:405 _cleanupRequests) | ACCEPTED | `~LLAssetStorage()`(:359 `_cleanupRequests(true, LL_ERR_CIRCUIT_GONE)`)= `delete gAssetStorage`(llappviewer.cpp:2388)= shutdown 時に in-flight `.material` 等の pending request を一括掃除する benign cleanup(E-13 shutdown-residual 同型)。"aborted"(all=true)のみ本 pattern・"timed out"(all=false=実 timeout)は不 match で残置 | AYA | 2026-08-07 |
| `getCapability called before caps received for (SimulatorFeatures\|ModifyRegion)`(llviewerregion.cpp:3755 getCapability) | ACCEPTED | 処理欠陥なし = 正しい正常動作への警報(E-02 型の警報定式問題)。警報条件(:3753)は `capabilitiesReceived()` フラグのみ判定し map(値の有無)を見ない。全枝トレース: (A)SimulatorFeatures 27件 = seed caps 受信ループ内で setCapability(:3675 `mCapabilities["SimulatorFeatures"]=url`)直後に requestSimulatorFeatures→getCapability(:2585)を呼ぶが、フラグを RECEIVED にする唯一の代入 setCapabilitiesReceived(true)(:403)はループ完了後 → 警報時フラグ off だが値は map 在 = 機能正常(url 返り coro 起動)。(B)ModifyRegion 51件 = RegionHandshake(unpackRegionHandshake:3424)が seed caps 完了前に着き ModifyRegion 未着 = 空返し → sRefreshFromRegion fallback(:3427)で正しく処理。両経路とも早期呼び出しは safe(A=値あり / B=fallback あり)。名前スコープ限定 = 他 cap の未処理な早期使用(真 defect)は不 match で alarm 残置 = 検出器を盲目化しない。upstream LL_WARNS・VK 非関係 | AYA | 2026-08-07 |
